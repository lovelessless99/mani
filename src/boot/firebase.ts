// Firebase boot file - configuration will be added in Task 2
import { boot } from 'quasar/wrappers'

export default boot(async (/* { app, router, store } */) => {
  // Firebase initialization will be configured here in Task 2
})
